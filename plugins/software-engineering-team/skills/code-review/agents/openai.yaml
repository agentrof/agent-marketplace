interface:
  display_name: "Code Review"
  short_description: "Review framework for the software-engineering-team code reviewer. Defines the finding contract, severity taxonomy, verdict rollup, the single evolving finding record, and the three review passes. Loaded by the reviewer agent during review loops, never invoked directly."
policy:
  allow_implicit_invocation: true
