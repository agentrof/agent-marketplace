interface:
  display_name: "Docker Compose"
  short_description: "Containerized environment expertise loaded by software-engineering-team agents for environment work. Use when defining or changing the project's containerized environment, authoring image build recipes, wiring healthchecks and service dependencies, designing deterministic seed scenarios, isolating parallel environment instances, or verifying a one-command full-stack bring-up with clean logs."
policy:
  allow_implicit_invocation: true
