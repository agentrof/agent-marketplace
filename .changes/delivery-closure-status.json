{
  "summary": "Recording a Delivery PR now moves the Delivery to awaiting_merge in the PR head itself, and the Delivery compiler reports it as merged once the current branch reaches a merge of that exact recorded head, including through a target refresh, so a merged Delivery keeps its pinned historical sources instead of failing its check after a later backlog revision.",
  "components": {
    "software-engineering-team": "patch"
  }
}
