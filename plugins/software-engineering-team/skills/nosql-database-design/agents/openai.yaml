interface:
  display_name: "Nosql Database Design"
  short_description: "Document database design expertise, loaded by software-engineering-team agents for database work. Document modeling, embedding vs referencing, schema patterns, indexing, sharding, and consistency for document stores. Loaded by the architecture role when the project stack declares a document database."
policy:
  allow_implicit_invocation: true
