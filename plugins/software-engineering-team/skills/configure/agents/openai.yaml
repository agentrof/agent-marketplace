interface:
  display_name: "Configure"
  short_description: "The single change gate for the machine-managed project config. Interprets a requested change, validates it against the supported enums, shows the impact, and writes only workspace/config.json on approval."
policy:
  allow_implicit_invocation: false
