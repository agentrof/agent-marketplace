# Materialized by the software-team setup entry; {{test_command}} is
# replaced with the project's configured test command.
name: tests

on:
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run the configured test suite
        run: {{test_command}}
