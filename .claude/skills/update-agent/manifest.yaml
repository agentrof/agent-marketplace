id: update-agent
kind: skill
version: 0.1.0
description: Dependency-aware mutator for a subagent file at .claude/agents/<id>.md. Runs mechanical scans plus advisory semantic review plus epistemic loop before any write.
tags:
  - mutation
  - primitive
  - deep-loop
depends_on:
  - create-agent
  - list-agents
