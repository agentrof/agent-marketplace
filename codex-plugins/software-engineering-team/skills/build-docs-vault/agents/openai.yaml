interface:
  display_name: "Build Docs Vault"
  short_description: "Run the Build Docs Vault Agentrof workflow"
  default_prompt: "Use $build-docs-vault to start this Agentrof workflow."
policy:
  allow_implicit_invocation: false
