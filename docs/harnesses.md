# Harness support

The source of truth is the Claude Code tree under plugins/. Every other
install surface is generated by tools/generate.py from
tools/data/harnesses.json; generated files are never hand-edited and CI
fails on drift.

<!-- harness-matrix:start -->
| Capability | Claude Code | Cursor | Codex CLI |
|---|---|---|---|
| Install channel | plugin marketplace add, plugin install | team marketplace import from the repository, or local plugin symlink; marketplace publication planned | codex plugin marketplace add, then the plugins browser |
| Skills | native source tree | native source tree (Agent Skills standard) | native source tree via the plugin skills pointer |
| Agents | native source tree | generated markdown tree, model inherit, readonly for restricted roles | generated TOML tree synced into the personal agents directory by setup |
| Entry-skill encapsulation | disable-model-invocation | disable-model-invocation honored | openai.yaml allow_implicit_invocation false |
| Knowledge-skill hiding | user-invocable false | not supported; skills appear in the menu | not supported; skills appear in the menu |
| Hooks | native, deny before write | generated manifest, camelCase events, failClosed guards, deny before write | generated manifest; guardrail coverage only (shell, patch, MCP); user must trust via the hooks review |
| Session end accounting | SessionEnd hook | sessionEnd hook | absent; reconciled at the next session start |
| Enforcement without hooks | not applicable | detect-after mode: integrity tripwire plus gate checks | detect-after mode: integrity tripwire plus gate checks |
| Dashboard | launched by the control-tower entry | hand-launched in the user's terminal (sandbox blocks local binding) | hand-launched in the user's terminal (sandbox blocks local binding) |
| Model policy | alias enum in agent frontmatter | all aliases resolve to inherit; the session model governs | all aliases resolve to inherit; the session model governs |
| Windows | supported | out of scope (POSIX shell commands) | out of scope (POSIX shell commands) |
<!-- harness-matrix:end -->
