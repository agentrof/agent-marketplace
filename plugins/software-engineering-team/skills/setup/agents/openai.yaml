interface:
  display_name: "Setup"
  short_description: "Idempotent project bootstrap for the software-engineering-team. Creates the workspace skeleton, materializes templates, and builds the machine-managed config interactively. Safe to re-run; never breaks what exists."
policy:
  allow_implicit_invocation: false
