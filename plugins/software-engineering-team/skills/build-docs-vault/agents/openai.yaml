interface:
  display_name: "Build Docs Vault"
  short_description: "On-demand reorganization of the whole workspace docs vault. Full-width audit, an owner-gated rename batch, deterministic migration, re-rendered generated views and in-session curation, closing on a green re-check. The vault librarian; scoped stewardship at each docs gate stays where it is."
policy:
  allow_implicit_invocation: false
