interface:
  display_name: "Sketch"
  short_description: "Run the Sketch Agentrof workflow"
  default_prompt: "Use $sketch to start this Agentrof workflow."
policy:
  allow_implicit_invocation: false
